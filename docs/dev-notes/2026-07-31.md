# 2026-07-31

## Scope

Execute PLAN-1 and most of what it depends on: make Parquet the durable raw
zone with ingestion writing it directly, split warehouse loading into its own
idempotent step, make DuckDB the default dbt target with BigQuery a named one,
add staging models for the three unmodelled sources, add a metadata mart, turn
on docs generation, and make CI a real end-to-end run rather than a compile.

Out of scope: the analysis marts in `dbt/models/marts/README.md`, which are
deliberately the human's to write, and PLAN-1 step 5, off-machine Parquet
storage, which still needs its own ADR.

## Changes

**Ingestion, rewritten around the raw zone (ADR-4).**

- New `ingestion/raw_zone.py`. Owns the layout
  (`data/raw/<table>/ingest_date=.../part-<run_id>-<seq>.parquet`) and is the
  only thing that reads it. `read_sql()` is the single reader, so the three
  load-bearing DuckDB options live in one place.
- `ingestion/ingest.py` now writes Parquet and nothing else, needs no
  credentials, and reads its watermark from the zone. Gained `--fixtures` so
  CI can run it with no network, and `--full-refresh` now rebuilds into a
  temporary tree and swaps it in, rather than truncating in place.
- New `ingestion/load.py`. Reads the zone into DuckDB or BigQuery with replace
  semantics. Both targets read through the same DuckDB reader, so the two
  warehouses cannot disagree about what the zone contains.
- Run manifests: every run writes `_runs/<run_id>.json`, loaded as
  `raw_datasf.raw_ingest_runs`.
- `ingestion/export_parquet.py` deleted. PLAN-1 step 8.

**dbt.**

- `profiles.yml`: `duckdb` is the default target, BigQuery renamed `dev` ->
  `bigquery`. Callers updated in the Makefile and `dbt.yml`.
- `cross_engine.sql` gained `x_safe_int`, `x_json_extract_scalar`,
  `x_hours_between` and `x_utc_now`. Six logical macros now, against ADR-1's
  revisit threshold of about ten.
- New `macros/audit_run_results.sql` plus `on-run-start`/`on-run-end` hooks,
  persisting dbt's own results to `meta_dbt_run_results`.
- Three staging models: `stg_datasf__building_permits`,
  `stg_datasf__city_budget`, `stg_datasf__film_locations`, all following the
  311 source/deduplicated/renamed shape, with yml, column tests and an
  explicit grain sentence each.
- New `mart_pipeline_freshness`, one row per source, generated by looping over
  a new `var('pipeline_sources')` registry in `dbt_project.yml`.
- Two changes to the reference 311 model, called out because it is the model
  everything else is copied from: an `_ingested_at` tiebreak in the dedup so
  the winner is deterministic, and `supervisor_district` now cast to an
  integer so it joins to the other staging models.

**Everything else.**

- `Makefile` restructured around ingest -> load -> build, with `rebuild`,
  `ci-build`, `docs` and `compile-bigquery` added.
- `ci.yml` replaced the compile-only DuckDB job with a real end-to-end run
  from fixtures, plus a credential-free BigQuery compile job. `ingest.yml`
  rewritten for the two-step pipeline. `dbt.yml` retargeted.
- `tests/fixtures/` with committed Socrata JSON and the generator that makes
  it. `docs/dbt/` with committed `manifest.json` and `catalog.json`.
- ADR-4 written. PLAN-1 updated with what is done, dropped and still open.
- CLAUDE.md rewritten around the three-step pipeline, and given a rule
  forbidding Claude from committing or pushing.

## Findings

**Ingestion was silently losing rows, and had been from the start.** `$order`
was `:updated_at` with `$offset` paging. DataSF bulk-refreshes these datasets,
so ties in `:updated_at` are enormous: in one 36,112-row slice of
`building_permits`, 36,095 rows shared an `:updated_at` with another row and
the largest single tie was 7,425 rows, spanning two page boundaries. Ordering
by a non-unique column gives the API no reason to return a stable sequence
across separate requests, so pages overlap and, worse, leave gaps. The slice
contained 35,918 distinct `record_id` values against an upstream dataset where
`record_id` is unique across all 1,292,923 rows: 194 rows duplicated, and by
implication about as many never fetched at all. Fixed by ordering on
`(:updated_at, :id)`, which is a total order. Re-ingesting the same slice
afterwards gave 36,112 rows and 36,112 distinct `record_id`. This produced no
error and no warning, and the duplicates it created were invisible because the
staging model deduplicates them away.

**A freshness view was going to be seven hours wrong.** DuckDB's `now()` is a
`TIMESTAMP WITH TIME ZONE`, but `x_safe_cast(col, 'timestamp')` on an
offset-bearing string yields a naive UTC timestamp, because casting to
DuckDB's `TIMESTAMP` keeps the UTC wall clock and drops the offset. Subtract
one from the other and DuckDB converts to local time, so `hours_since_load`
measured -6.8 on a laptop in PDT: data appearing to arrive from the future.
BigQuery has no equivalent trap, its `TIMESTAMP` being an absolute instant, so
this would have been a DuckDB-only wrong answer in the one view whose whole
job is to be trusted at a glance. Hence `x_utc_now()`.

**`record_id`, not `permit_number`, is the permit grain.** `permit_number`
repeats up to 101 times, because revisions and addenda are filed as separate
records against one permit. A unique test on `permit_number` would have failed
on the first real backfill. Similarly, `city_budget` has no natural key at all:
the full combination of year, department, fund, program, object, sub_object
and character still returns groups of up to six, so the grain key is Socrata's
row id. Same for `film_locations`, where the same title at the same address
appears up to three times with nothing to tell the rows apart.

**A 15-row `LIMIT` nearly shipped a broken test.** The first pass at
`accepted_values` for `permit_status` came from a top-15 query and missed
`issuing`, `denied`, `inspection`, `incomplete`, `unknown`, `upheld`,
`granted` and `overruled`, all with single-digit or low-double-digit counts,
plus 12 rows where `status` is null. The local slice happened to contain three
of the missing values, which is the only reason it surfaced now rather than on
the first full backfill.

**Small samples cannot be fixtures here.** Socrata omits null fields per
record rather than sending nulls, so a column present in 2% of rows is simply
absent from a 25-row sample and the resulting Parquet file has no such column
at all. `stg_datasf__building_permits` failed with "Referenced column
street_number_suffix not found". That failure is correct in production, where
a column vanishing upstream should stop the build rather than quietly become
NULL, so the fix belongs in the fixtures and not in the model: the generator
now appends a synthetic coverage record carrying every field seen in a 400-row
scan. This is exactly the kind of thing that would otherwise be discovered by
a staging model breaking on a quiet Tuesday.

**`make check` was contaminating the raw zone, and I shipped it before
catching it.** `ci-build` ran the fixture ingest against the default zone, so
running the local CI gate appended a fixture row to `data/raw`. Caught by
diffing row counts after the first `make check`: `raw_film_locations` had gone
from 2,214 to 2,215, because the coverage record's `:updated_at` was newer
than that dataset's watermark. The other three were unaffected only by luck of
their watermarks being newer. `ci-build` now runs entirely inside `data/ci/`.
The zone was restored by deleting the four fixture-mode run manifests and the
one Parquet file, and verified back to its original content hash.

**The DuckDB path in `profiles.yml` is resolved against the caller's working
directory.** `../data/sf.duckdb` is right when dbt runs from `dbt/`, which is
what the Makefile does, and wrong from anywhere else. sqlfluff runs the dbt
templater from the repo root and was trying to open
`/Users/mp/projects/data/sf.duckdb`, a sibling of the repository, and crashing
with a raw `IOException` rather than anything mentioning configuration. The
Makefile and CI now export an absolute `DUCKDB_PATH`.

**`dbt compile --target bigquery` needs no credentials.** Compiling does not
open a connection, so the entire BigQuery dialect can be checked on a fork
pull request. This was assumed impossible when `ci.yml` was written, which is
why the cross-engine guarantee had no gate. It has one now.

**Jinja's `match` test does not exist in dbt's sandbox.** Used it to filter
node ids in the on-run-end hook. The hook compiles last, so the failure
arrived after a full successful build, reporting "No test named 'match'" with
`ERROR` and an otherwise green run above it.

## Not done

- **Nothing committed or pushed.** Every change is in the working tree. This
  is now a standing rule in CLAUDE.md rather than a per-session choice.
- **BigQuery still never executed.** No credentials in this session either.
  Both targets compile and the BigQuery branches render correct dialect, but
  `dbt build --target bigquery` has not run, and PLAN-1 step 4, comparing the
  two engines row for row, remains open. This is the largest untested surface
  in the repo.
- **Only slices of the large datasets are ingested locally.** 311 from
  2026-07-01 (103,266 rows) and permits from 2026-01-01 (36,112). A full
  backfill of 311 alone is 8,801,296 rows and was not pulled onto this machine
  unprompted. `make ingest` uses the registry start dates and will fetch the
  lot.
- **The raw zone still does not survive the machine.** The scheduled workflow
  caches it, which has a 7-day eviction and a 10 GB ceiling. PLAN-1 step 5.
- **`meta_dbt_run_results` is never pruned.** One row per node per run,
  forever.
- **PLAN-2 is only partly closed.** The ruff exemptions on `ingest.py` are
  gone, because the underlying issues were fixed rather than silenced, but the
  `datasets.py` module-shadowing hazard remains; `known-first-party` fixes the
  lint, not the collision.

## Verification

dbt 1.12.0, dbt-duckdb 1.10.1, dbt-bigquery 1.12.0, duckdb 1.5.5, ruff 0.16.1,
sqlfluff 4.2.2. Note dbt-duckdb was not installed in `.venv` at the start of
this session despite being in `requirements.txt`, so `make compile-duckdb`
could not have passed locally as PLAN-1 step 1 claims.

- `make check` passes: ruff check, ruff format, sqlfluff lint, leak-check,
  BigQuery compile, and the full fixture pipeline. Verified afterwards by
  content hash that it leaves `data/raw` untouched.
- `make build` against real data: PASS=35, WARN=0, ERROR=0.
- **The acceptance criterion.** Captured an order-independent content hash of
  every staging model plus the stable columns of the mart, deleted
  `data/sf.duckdb` entirely, ran `make rebuild`, and re-hashed. All four
  staging models and the mart matched exactly. Repeated after cleaning the
  fixture contamination, with the same result.
- Adversarial fixture rows verified individually: deduplication keeps the
  later of two versions of a case, unparseable coordinates and costs become
  NULL rather than erroring, a permit with no `location` yields NULL
  coordinates rather than failing the JSON extraction, a negative budget
  amount survives, and a film with no release year survives.
- Cross-engine output inspected directly: the BigQuery-compiled SQL contains
  `safe_cast`, `float64`, `int64`, `json_value` and `timestamp_diff`, and no
  `try_cast`, `json_extract_string`, `date_diff` or `at time zone` outside a
  comment.
- Watermark resume confirmed against the live API: a second run of
  `film_locations` fetched zero rows and recorded a manifest saying so, which
  is what `mart_pipeline_freshness` reports as `row_delta` 0 rather than as
  staleness.

## Follow-ups

1. Run `make load-bigquery` and `make build-bigquery` by hand with real
   credentials, then do PLAN-1 step 4, the row-for-row comparison. Until that
   happens "compiles on both engines" is all anyone can honestly claim.
2. Write the ADR for durable Parquet storage. It is the last thing blocking
   PLAN-1 from closing, and the Actions cache is a mitigation wearing an
   answer's clothes.
3. Decide whether `mart_pipeline_freshness` should assert rather than report:
   it can see that the run manifests and the row counts disagree, and
   currently only displays it.
4. Consider a scheduled `--full-refresh` on `building_permits`, now that the
   tie bug is known to have been dropping rows. The existing zone was ingested
   with the broken ordering and is missing rows nothing will ever backfill.
5. The marts in `dbt/models/marts/README.md` are still unwritten, and ADR-3
   makes both core sources having a mart the trigger for revisiting scope.

## Second session: lint toolchain drift

Separate from the work above, and only about the quality gates.

### Symptom

`git commit` failed in `sqlfluff-lint` with a traceback that ended in
`_ForkingPickler.dumps` and never named a rule or a file. The models were
fine. The hook's own environment could not be built.

### Cause

pre-commit builds an isolated env per hook from `additional_dependencies`.
That env is a second toolchain, and nothing in this repo constrained it.
It pinned `dbt-core==1.9.1`, which caps `mashumaro` below 3.15, and
mashumaro 3.14 does not import on Python 3.14, where PEP 649 deferred
annotations changed how its type introspection resolves. The real error was
`UnserializableField: Field "schema" of type Optional[str]`, buried under
multiprocessing trying to pickle the worker exception.

CI never saw it. The lint job runs Python 3.11 and installs sqlfluff from
`requirements-dev.txt`, so it was resolving sqlfluff 4.2.2 and dbt-core
1.12.0 while the hook was frozen at 3.3.0 and 1.9.1. Three toolchains, one
of them broken, and the broken one was the only one a human ran.

### Changes

- New `scripts/sqlfluff-lint.sh`. Runs the venv's sqlfluff against
  `dbt/models`, sets `DBT_PROFILES_DIR` and an absolute `DUCKDB_PATH` itself
  so it works from a shell with nothing exported, and honours an existing
  `DUCKDB_PATH` so `.env` still steers it.
- `.pre-commit-config.yaml`: sqlfluff is a `language: system` local hook now,
  beside leak-check, with no version pins of its own. It cannot drift from
  CI because it resolves from the same venv.
- The hook triggers on `dbt/(models|macros)/**.{sql,yml}`, wider than what it
  lints. A macro change alters what every model compiles to and previously
  did not re-run the lint.
- `make lint` calls the script instead of restating the command.

### The part that mattered more

Bumping the ruff pin for consistency surfaced a live failure. The hook pinned
`v0.8.6`; `requirements-dev.txt` says `ruff>=0.8`, which now resolves 0.16.1.
Ruff 0.16 stabilised PLC0415, and `ingestion/load.py` has two deliberate
function-scope `from google.cloud import bigquery` imports. So `make lint` and
the CI lint job were both failing on HEAD while the pre-commit hook passed.
Silenced per line with the reasoning at the first occurrence, rather than
ignoring PLC0415 repo wide, because the rule is right everywhere else in
`ingestion/`. Hook is now `ruff-check` at v0.16.1, matching what CI installs.

### Follow-ups

1. Done in this session. Ruff keeps its own pre-commit environment, which is
   what ruff upstream recommends and which is safe for ruff in a way it was
   not for sqlfluff: ruff is a single binary with no dependency graph, so a
   pinned rev cannot fail to resolve the way a pinned dbt-core did. What was
   unsafe was the float, so `requirements-dev.txt` now says `ruff==0.16.1`
   and `scripts/check-lint-pins.sh` fails the commit if that version and the
   hook rev disagree. It runs as a hook, in `make lint`, and as the first
   step of the CI lint job, before the linters whose output it qualifies.
   sqlfluff is left floating on purpose: it has only one install now, so
   there is nothing for it to disagree with.
2. The CI lint job installs its own sqlfluff rather than calling
   `scripts/sqlfluff-lint.sh`, so `dbt/models --processes 4` is still written
   in two places. The script's venv fallback would warn on every CI run, which
   is why it was left alone.
3. The project venv is Python 3.14 and CI is 3.11. Nothing depends on the
   difference today, but it is how a broken hook stayed invisible.

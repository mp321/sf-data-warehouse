# 2026-07-30

## Scope

Make the repo readable and runnable by someone arriving cold: CLAUDE.md as the
single source of truth, a docs structure for plans, decisions and session
notes, ADRs for the architecture we are about to depend on, developer
ergonomics (Makefile, lint, leak check), and CI gating pull requests.
Explicitly out of scope: refactoring ingestion, adding datasets.

## Changes

**Docs and context.**

- `CLAUDE.md` at root, under 150 lines. Carries the working agreement at the
  top so Claude Code in the VS Code extension reads the same rules the
  claude.ai project prompt used to supply: plans to `docs/plans/`, decisions to
  `docs/decisions/`, session notes to `docs/dev-notes/`, no emojis, no em or en
  dashes. Includes a current-versus-intended state table, because the ADRs
  describe where we are going and will otherwise be read as descriptions of
  what runs today.
- `docs/README.md` covering the three document types and why plans and dev
  notes stay separate.
- Templates in each docs folder.
- ADR-1 (warehouse targets), ADR-2 (spatial strategy), ADR-3 (dataset scope),
  all `status: active`.
- PLAN-1 (DuckDB and Parquet migration) and PLAN-2 (remove the ruff exemptions
  on ingestion).

**Cross-engine SQL.**

- New `dbt/macros/cross_engine.sql` with `x_type`, `x_safe_cast` and `x_cast`
  dispatching per adapter.
- `stg_datasf__311_cases.sql` calls those macros instead of `safe_cast` and
  `float64`.
- `dbt/profiles.yml` gained a `duckdb` output. BigQuery stays the default.

**Ergonomics and safety.**

- `Makefile` with setup, ingest, build, test, docs, lint, fmt, leak-check,
  compile-duckdb, build-duckdb, export-parquet, check, rebuild, clean.
- `.gitignore` rewritten. See findings.
- `scripts/leak-check.sh`, `.pre-commit-config.yaml` (ruff, sqlfluff,
  detect-private-key, leak-check), `ruff.toml`, `.sqlfluff`.
- `requirements.txt` gained both dbt adapters with upper bounds;
  `requirements-dev.txt` added.
- `ingestion/export_parquet.py`, standalone and opt-in. Nothing calls it.

**CI.**

- New `.github/workflows/ci.yml`: leak-check, lint, and dbt parse plus compile
  against DuckDB, on every PR and push to main. Credential-free, so it works
  on fork PRs.
- `dbt.yml` and `ingest.yml` keep their schedules and now drop the service
  account key in an `if: always()` step.

## Findings

**The `.gitignore` ignored itself.** It listed `.gitignore`, `README.md`,
`SETUP.md`, `PLAN.md` and `.env.example` among its entries, so none of them
were ever committed and `git ls-files` returned 10 files. The effect: a fresh
clone had no ignore rules at all, so the first person to follow SETUP.md, drop
a key into `keys/sa.json` and run `git add .` would have committed a live
service account private key. The ignores worked on this machine, and every
blob in history checks clean, so this was a loaded trap rather than a fire.
It is why `scripts/leak-check.sh` scans untracked-but-not-ignored files rather
than trusting the ignore rules it backstops.

**The one dbt model had never parsed.** Line 6 of
`stg_datasf__311_cases.sql` was a SQL comment containing `{{ source(...) }}`.
Jinja runs before SQL parsing and does not know that `--` starts a comment, so
it evaluated that and died on the literal `...` with "unexpected '.'".
Confirmed against the committed version at HEAD, not just the local edit:
`dbt parse` fails on the original file. So the weekly `dbt` workflow has failed
on every run since the initial commit, unnoticed, because a failing scheduled
workflow is a red icon on a tab nobody opens. Fixed by wrapping the header
comment in `{% raw %}`. A scheduled build providing no signal is worse than no
scheduled build, and is the main argument for the new PR gate.

**leak-check had a silent hole, caught only by a negative test.** The private
key pattern starts with `-----BEGIN`, and `grep -E "$regex"` parses a leading
`-----` as options rather than a pattern, so the most important rule in the
file matched nothing. It needs `grep -E -e "$regex" -- "$f"`. Found by
planting six fake secrets in a scratch repo and checking all six were flagged;
five were. A security script never shown to fail on a real positive is
decoration.

**A macro that was right by accident.** `bigquery__x_safe_cast` called the
dispatching `x_type` rather than `bigquery__x_type`. Dispatch resolves on the
connected adapter, so invoking the BigQuery branch while connected to DuckDB
produced `safe_cast(lat as double)`: a BigQuery function with a DuckDB type
name, valid on neither engine. The two normally agree, so this would only have
surfaced years later during a migration. Each branch now calls its own type
macro.

**`dbt-core>=1.8` is not a safe constraint.** A clean install resolved to a
dbt-core 2.0 alpha whose metadata generation downloads a wheel from GitHub
releases, which fails on any machine that cannot reach github.com with an
error that looks nothing like a version problem. Pinned to `<2.0`. Note that
the existing `.venv` contains `dbt-core-experimental-parser`, so that alpha is
already installed locally, which may explain behaviour differences against CI.

**Alias alignment and macros are incompatible.** The model aligned `as
<alias>` in a column. sqlfluff lints rendered SQL, and macro expansions are
not the width of their source text, so source-aligned code arrives at the
linter misaligned and hand-tuning cannot fix it. Switched to a single space
before `as`, with the reason recorded in `.sqlfluff`.

**Smaller things.** `get_watermark` takes `max()` of `_socrata_updated_at`, a
STRING column, and is correct only because Socrata's ISO-8601 timestamps are
fixed width and therefore sort lexically. Nothing documents that this is load
bearing. And `ingest.py`'s `from datasets import DATASETS` resolves only
because Python puts the script's directory on `sys.path`; it will collide with
the HuggingFace `datasets` package if that ever lands in the venv.

## Not done

- No ingestion logic changes. `ingest.py` was not touched.
- No new datasets.
- Nothing committed or pushed. Every change is in the working tree for review.
- **The BigQuery target was never executed.** No credentials in this session.
  The BigQuery macro branch was verified to render `safe_cast(... as float64)`
  by calling it directly, and the project parses with the BigQuery adapter
  registered, but `dbt build --target dev` has not been run. Given the model
  had never parsed before today, the first real BigQuery run is unknown
  territory and should be done by hand rather than left to the Monday
  schedule.
- Three ruff findings in `ingestion/` are exempted rather than fixed, since
  fixing them means touching ingestion. Tracked in PLAN-2, with the reasoning
  inline in `ruff.toml`.
- `make build` still targets BigQuery and still needs credentials. A
  credential-free full build needs the Parquet raw zone, which is PLAN-1.

## Verification

Against the final state of the tree, using dbt 1.9.10 and dbt-duckdb 1.9.6:

- `leak-check.sh` clean on the repo; separately confirmed it flags planted
  private keys, GCP service account JSON, AWS keys, Anthropic keys, database
  URLs with passwords, forbidden filenames, and a self-ignoring `.gitignore`.
- `ruff check` and `ruff format --check` pass.
- `sqlfluff lint dbt/models` passes with the dbt templater against DuckDB.
- `dbt parse` and `dbt compile --target duckdb` succeed.
- `dbt build --target duckdb` against throwaway fixture data in `/tmp`:
  PASS=5, ERROR=0. The fixtures deliberately included a duplicated case and
  unparseable coordinates; dedup kept the newer version and the bad
  coordinates became null rather than erroring.
- All workflow and dbt YAML files parse.

## Follow-ups

1. Run `make build` against BigQuery by hand and confirm it goes green. This
   is what separates "CI is green" from "CI is green and means something".
2. Work `docs/plans/plan-1-duckdb-parquet.md`, which turns `make setup && make
   build` into a credential-free operation on a fresh clone.
3. Update `README.md`. It advertises the budget analysis as the headline and
   presents four datasets as equals, both contradicted by ADR-3. It is also
   still untracked, so committing it is part of this.
4. Delete `PLAN.md`, a 0 byte file at root superseded by `docs/plans/`.
5. Add a status badge for the `ci` workflow, and consider a notification on
   scheduled workflow failure so the next silent breakage is not silent.
